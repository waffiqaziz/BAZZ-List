<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Base application theme. -->
  <style name="Theme.BAZZList" parent="Theme.Material3.Light.NoActionBar">
    <item name="fontFamily">@font/nunito_sans_regular</item>

    <!-- Primary brand color. -->
    <item name="colorPrimary">@color/gray</item>
    <item name="colorPrimaryVariant">@color/yellow</item>
    <item name="colorOnPrimary">@color/white</item>
    <item name="colorAccent">@color/gray_800</item>
    <item name="colorOnBackground">@color/gray_900</item>

    <!-- Secondary brand color. -->
    <item name="colorSecondary">@color/teal_200</item>
    <item name="colorSecondaryVariant">@color/teal_700</item>
    <item name="colorOnSecondary">@color/black</item>

    <!-- Status bar color. -->
    <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    <item name="android:windowLightStatusBar">false</item>
  </style>

  <style name="Theme.BAZZList.AppBarOverlay" parent="ThemeOverlay.Material3.Toolbar.Surface" />

  <style name="Theme.BAZZList.PopupOverlay" parent="ThemeOverlay.Material3.Light" />

  <style name="SplashScreenTheme" parent="Theme.SplashScreen">
    <item name="windowSplashScreenAnimatedIcon">@drawable/splash_inset</item>
    <item name="windowSplashScreenBackground">@color/gray</item>
    <item name="windowSplashScreenAnimationDuration">1000</item>
    <item name="postSplashScreenTheme">@style/Theme.BAZZList</item>
  </style>

  <style name="MyThemeOverlay_Toolbar" parent="ThemeOverlay.Material3.Toolbar.Surface">
    <item name="colorOnPrimary">@color/gray_900</item>
    <item name="colorControlNormal">@color/gray_900</item>
    <item name="fontFamily">@font/nunito_sans_bold</item>
  </style>

  <style name="HeaderNavigationText">
    <item name="android:textColor">@color/gray_900</item>
  </style>

  <style name="ButtonWarning" parent="Widget.Material3.Button.TonalButton">
    <item name="android:textAllCaps">false</item>
    <item name="android:layout_width">wrap_content</item>
    <item name="android:layout_height">wrap_content</item>
  </style>
</resources>